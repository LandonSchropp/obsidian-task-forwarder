import { Editor } from "obsidian";

/**
 * Forward tasks from the previous daily notes to the current daily note.
 * @param editor The Obsidian editor to use.
 */
export function forwardTasks(editor: Editor): void {}
